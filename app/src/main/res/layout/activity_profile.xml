<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/c_four">

    <LinearLayout
        android:id="@+id/containerCenter"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="24dp"

        android:layout_marginStart="@dimen/margen_perfil"
        android:layout_marginEnd="@dimen/margen_perfil"

        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">


        <!-- TITULO -->
        <TextView
            android:id="@+id/txtTitulo"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Usuario"
            android:textSize="@dimen/tamano_user"
            android:textStyle="bold"




            android:textColor="@android:color/black"
            android:layout_marginBottom="24dp"/>

        <!-- IMAGEN MÁS GRANDE -->
        <ImageView
            android:id="@+id/imgUsuario"
            android:layout_width="@dimen/tamano_avatar"
            android:layout_height="@dimen/tamano_avatar"
            android:background="@drawable/circle_bg"
            android:clipToOutline="true"
            android:outlineProvider="background"
            android:src="@drawable/ic_user"
            android:scaleType="centerCrop"
            android:layout_marginBottom="36dp"/>

        <!-- BOTONES MÁS ANCHOS -->
        <Button
            android:id="@+id/btnDatos"
            android:layout_width="match_parent"
            android:text="Datos Personales"
            android:layout_height="@dimen/tallButton"
            android:textSize="@dimen/buttonFontSize"
            android:layout_marginBottom="16dp"
            android:textColor="@color/c_two"
            android:backgroundTint="@color/c_three"/>

        <Button
            android:id="@+id/btnNotificaciones"
            android:layout_width="match_parent"
            android:text="Notificaciones"
            android:layout_height="@dimen/tallButton"
            android:textSize="@dimen/buttonFontSize"
            android:layout_marginBottom="16dp"
            android:textColor="@color/c_two"
            android:backgroundTint="@color/c_three"/>

        <Button
            android:id="@+id/btnTemas"
            android:layout_width="match_parent"
            android:text="Temas de colores"
            android:layout_height="@dimen/tallButton"
            android:textSize="@dimen/buttonFontSize"
            android:layout_marginBottom="28dp"
            android:textColor="@color/c_two"
            android:backgroundTint="@color/c_three"/>

        <Button
            android:id="@+id/btnCerrarSesion"
            android:layout_width="match_parent"
            android:text="Cerrar Sesión"
            android:layout_height="@dimen/tallButton"
            android:textSize="@dimen/buttonFontSize"
            android:textColor="@android:color/white"
            android:backgroundTint="#F44336"/>

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
